/**
 * Clipboard history theme — card strip docked at the bottom, liquid-glass.
 * Entries are fed as "text\0icon\x1fcard.png" by cliphist-rofi.sh (SUPER+V).
 * Cards (130px logical, rendered 2x by the script) are pre-baked with rounded
 * corners + pango text, so each element is just the icon.
 * Colors come from pywal via ~/.cache/wal/colors-rofi.rasi so it tracks the
 * active wallpaper, matching the NixOS look.
 **/
@import "~/.cache/wal/colors-rofi.rasi"

* {
    accent:        @selected;
    card-bg:       #ffffff12;
    card-bg-alt:   #ffffff08;
    card-sel-bg:   #ffffff1f;
    text-col:      @foreground;
    text-dim:      #ffffff80;
    font:          "JetBrainsMono Nerd Font 10";
}

configuration {
    show-icons: true;
}

/*****----- Window -----*****/
window {
    transparency:     "real";
    location:         south;
    anchor:           south;
    fullscreen:       false;
    width:            1014px;
    y-offset:         -14px;
    border-radius:    16px;
    padding:          0px;
    background-color: @glass;
    cursor:           "default";
}

mainbox {
    padding:          12px;
    spacing:          10px;
    background-color: transparent;
    children:         [ "inputbar", "listview" ];
}

/*****----- Search bar -----*****/
inputbar {
    spacing:          8px;
    padding:          8px 14px;
    border-radius:    10px;
    background-color: @card-bg-alt;
    text-color:       @text-col;
    children:         [ "prompt", "entry" ];
}
prompt {
    enabled:          true;
    text-color:       @accent;
    background-color: transparent;
}
entry {
    placeholder:       "Search clipboard…";
    placeholder-color: @text-dim;
    text-color:        @text-col;
    background-color:  transparent;
    cursor:            text;
}

/*****----- Card strip -----*****/
listview {
    columns:       7;
    lines:         1;
    cycle:         true;
    dynamic:       true;
    scrollbar:     false;
    layout:        vertical;
    fixed-height:  true;
    fixed-columns: true;
    spacing:       8px;
    padding:       2px;
    background-color: transparent;
    text-color:    @text-col;
}

/*****----- Cards -----*****/
element {
    padding:          0px;
    border:           2px solid;
    border-radius:    14px;
    border-color:     transparent;
    background-color: transparent;
    text-color:       @text-col;
    cursor:           pointer;
    children:         [ "element-icon" ];
}
element alternate.normal {
    background-color: transparent;
}
element selected.normal {
    background-color: transparent;
    border-color:     @accent;
    text-color:       @text-col;
}
element-icon {
    size:             130px;
    horizontal-align: 0.5;
    vertical-align:   0.5;
    background-color: transparent;
    cursor:           inherit;
}
element-text {
    enabled: false;
}

/*****----- Messages -----*****/
error-message {
    padding:          12px;
    border-radius:    10px;
    background-color: @glass;
    text-color:       @text-col;
}
